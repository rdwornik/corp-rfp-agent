# Testing Standards

- pytest for all tests, never unittest
- Test files: tests/test_*.py
- Run: python -m pytest
- 151 tests passing
- Fixture files in tests/fixtures/ (Excel + Word test files)
- No silent failures — log warnings, raise on errors
